% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HoMiCorr.R
\name{.run_regressions}
\alias{.run_regressions}
\title{Run Pairwise Regressions Across Features (internal)}
\usage{
.run_regressions(
  feature.combos,
  data,
  reg.method,
  covariates,
  zero_prop_from_formula,
  zibr_time_ind,
  show_progress,
  snowopts = doSNOWopts
)
}
\arguments{
\item{feature.combos}{A list of feature index pairs to be analyzed.
Each pair specifies two columns in `data` to be used as dependent and
independent variables.}

\item{data}{A data frame containing the feature columns and covariates
for modeling.}

\item{reg.method}{A string specifying the regression method to use.
Options include `"zibr"`, `"gamlss"`, `"lm"`, and `"lmer"`.}

\item{covariates}{A string specifying the covariates to include
in the model formula.}

\item{zero_prop_from_formula}{A boolean indicating whether
zero proportions should be modeled
using the specified formula (relevant for ZIBR regression).}

\item{zibr_time_ind}{A string specifying the time variable for ZIBR models.}

\item{show_progress}{A boolean indicating whether to
display progress updates.}

\item{snowopts}{Options passed to `foreach::foreach()`
for parallel execution using the `doSNOW` backend.}
}
\value{
A data frame summarizing the regression results
for each feature pair.
}
\description{
This function iterates over all specified feature pairs
and performs regression analyses in parallel. The regression method
is determined by `reg.method`, and the function supports multiple
modeling approaches, including ZIBR, GAMLSS, linear models,
and linear mixed-effects models.
}
\examples{
# Example usage (not run):
# feature.combos <- list(c(1, 2), c(3, 4))
# results <- HoMiStats:::.run_regressions(feature.combos, data,
#                             "lm", "age + sex",
#                             NULL, NULL, TRUE)

}
\keyword{internal}
